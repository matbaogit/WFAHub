# ==============================================
# WFA Hub - Environment Variables Template
# ==============================================
# Copy file này thành .env và điền các giá trị thực

# ----------------------------------------------
# PostgreSQL Database Configuration
# ----------------------------------------------
# DATABASE_URL format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://wfahub_user:your-secure-password@localhost:5432/wfahub

# Individual PostgreSQL credentials (optional if using DATABASE_URL)
PGHOST=localhost
PGPORT=5432
PGUSER=wfahub_user
PGPASSWORD=your-secure-password
PGDATABASE=wfahub

# ----------------------------------------------
# Security & Encryption
# ----------------------------------------------
# Session secret cho Express session (tối thiểu 32 ký tự)
# Tạo bằng: node generate-secrets.js
SESSION_SECRET=your-random-session-secret-min-32-chars-change-this

# Encryption key cho mã hóa SMTP password (32 ký tự hex)
# Tạo bằng: node generate-secrets.js
ENCRYPTION_KEY=your-random-encryption-key-32-chars-change-this

# ----------------------------------------------
# Application Configuration
# ----------------------------------------------
# Môi trường: development | production
NODE_ENV=production

# Port để chạy server (mặc định: 5000)
PORT=5000

# ----------------------------------------------
# Puppeteer Configuration (Optional)
# ----------------------------------------------
# Đường dẫn đến Chromium executable (chỉ cần nếu auto-detect không hoạt động)
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium-browser

# ----------------------------------------------
# Lưu ý quan trọng
# ----------------------------------------------
# 1. KHÔNG commit file .env vào Git
# 2. Sử dụng script generate-secrets.js để tạo SESSION_SECRET và ENCRYPTION_KEY
# 3. Backup file .env vào nơi an toàn
# 4. Trên production, đảm bảo permissions: chmod 600 .env
